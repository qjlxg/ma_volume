name: 🧪 Strategy Backtest Analysis (Historical Success Rate)

on:
  # 仅通过手动触发运行回测，避免频繁大规模计算
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for running historical backtest'
        required: false
        default: 'Manual backtest request'
        
jobs:
  run_backtest:
    runs-on: ubuntu-latest
    
    defaults:
      run:
        shell: bash
    
    steps:
      - name: ⬇️ Checkout repository code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12' 
          
      - name: ⚙️ Install dependencies (pandas & ta)
        run: |
          python -m pip install --upgrade pip
          pip install pandas ta
          
      - name: 🏃 Run the Backtest Analysis Script
        # 调用回测脚本
        run: python backtest_analysis.py
        
      - name: 🔄 Commit and Push Backtest Report
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          # 追踪新的 backtest_results 目录
          file_pattern: 'backtest_results/**/*'
          commit_message: "🤖 Auto: Generated historical backtest report."
          commit_user_name: 'github-actions[bot]'
          commit_user_email: 'github-actions[bot]@users.noreply.github.com'
          token: ${{ secrets.GITHUB_TOKEN }}
          skip_dirty_check: false
