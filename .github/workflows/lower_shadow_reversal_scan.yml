name: 下影线反转

on:
  # 1. 手动触发
  workflow_dispatch:
  
  # 2. 脚本或工作流文件变动时自动触发
  push:
    branches:
      - main # 假设主分支是 main
    paths:
      - 'lower_shadow_reversal_scan.py'
      - '.github/workflows/lower_shadow_reversal_scan.yml'
    

  schedule:
    - cron: '0 15 * * *'

# 设置运行环境为上海时区，影响 runner 的日志和系统时间
env:
  TZ: 'Asia/Shanghai'

jobs:
  scan_and_commit:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      # 必须使用 fetch-depth: 0 才能在后续步骤中拉取远程更改并推送
      with:
        fetch-depth: 0 

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        # 安装 pandas, pytz
        pip install pandas pytz
        
    - name: Run Stock Scanner Script
      # 运行 Python 脚本
      run: python lower_shadow_reversal_scan.py
      
    - name: Configure Git for Commit
      run: |
        git config user.name 'github-actions[bot]'
        git config user.email 'github-actions[bot]@users.noreply.github.com'
        
    - name: Commit and Push Results (Fix for Rejected Push)
      # 检查是否有新的结果文件生成或现有文件被修改
      run: |
        git add --all
        
        # 检查是否有文件变动
        if git diff --cached --exit-code; then
          echo "No changes found. Exiting."
        else
          # 如果有变动，提交
          git commit -m "🤖 Auto scan: Lower Shadow Reversal results update."
          
          # **关键修复：在推送前先拉取并变基 (rebase)，解决 [rejected] 错误**
          # 这将确保远程更改被集成到本地提交之前，从而允许快进推送。
          git pull --rebase origin main
          
          # 推送
          git push
        fi
